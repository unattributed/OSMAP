# V10 Rust Assumption and Fail-Closed Audit

    ## Scope

    V10 Slice 4 classifies Rust assumptions that can affect fail-closed confidence. It is an audit and governance slice. It does not change product behavior, production deployment state, live-host configuration, authentication, mailbox handling, rendering, parsing, or request processing.

    ## Assessed source

    - Generated at UTC: `2026-06-25T09:38:24Z`
    - Local branch observed: `v10-rust-fail-closed-audit`
    - Local HEAD observed: `0e9b92c717ca553d16bd83eec6bf09f7a5e804cd`
    - `origin/main` observed: `0e9b92c717ca553d16bd83eec6bf09f7a5e804cd`
    - Local status clean before candidate generation: `true`

    ## Audit summary

    | Signal | Value |
    | --- | ---: |
    | Slice 0 carried-forward Rust assumption count | `712` |
    | Current normalized scanner count | `714` |
    | Inventory SHA256 | `eaea1f847a8b51dac00a82f25e8b117664ee22f7ed72576c10568769dc891ec4` |
    | Classification complete | `true` |
    | Product behavior changed | `false` |
    | Production state changed | `false` |
    | Live-host state changed | `false` |

    The current scanner count is generated by `maint/security/osmap-v10-rust-assumption-audit.py`. The Slice 0 count remains carried forward as the earlier intake signal. Future source changes must update the audit register or `make v10-check` will fail.

    ## Count by assumption kind

    | Kind | Count |
    | --- | ---: |
    | expect_call | `677` |
| panic_macro | `34` |
| unreachable_macro | `2` |
| unwrap_call | `1` |

    ## Count by fail-closed classification

    | Classification | Count |
    | --- | ---: |
    | control_flow_invariant | `2` |
| panic_path_assumption | `28` |
| production_adjacent_assumption | `608` |
| test_or_fixture_assumption | `76` |

    ## Count by fail-closed relevance

    | Relevance | Count |
    | --- | ---: |
    | high | `636` |
| low | `76` |
| medium | `2` |

    ## Classification policy

    - Test, fixture, example, and benchmark assumptions are recorded but treated as low relevance unless later used as release evidence.
    - Production-adjacent `unwrap` and `expect` assumptions are treated as high relevance until converted to explicit errors or fail-closed responses.
    - `panic!`, `todo!`, and `unimplemented!` in production-adjacent paths remain release-blocking for broader release claims until reviewed or remediated.
    - `unreachable!` is treated as a control-flow invariant that must be reviewed if user-reachable.

    ## This closure does not claim

    - all Rust assumptions are safe,
    - production request paths are panic-free,
    - every high-relevance assumption has been remediated,
    - broad public release readiness,
    - production deployment change.

    ## Required follow-on work

    1. Review high-relevance production-adjacent assumptions first.
    2. Convert confirmed user-reachable assumptions into explicit error handling or fail-closed responses.
    3. Add targeted regression tests for each remediated assumption.
    4. Preserve the V9 selected-cohort scope unless a later slice deliberately expands it with evidence.
