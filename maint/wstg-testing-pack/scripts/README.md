# Retired Script Directory

The previous numbered shell scripts were removed because they prompted for
secrets, produced ad hoc evidence, and did not map cleanly to OWASP WSTG v4.2
or ASVS 5.0.0 identifiers.

Use the top-level runner instead:

```bash
./run.sh
./run.sh --include-host
```
